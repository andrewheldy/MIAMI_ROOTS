-- Miami Roots local/staging seed — Milestone 3.
--
-- Intentionally empty of data. M3 establishes schema + security only; the
-- public site still renders from the in-code content module
-- (src/content/groups/). Public-safe group content is seeded here in
-- Milestone 4 — and per the standing rule, this file must NEVER contain
-- WhatsApp invite links, phone numbers, or any private community data.

select 1;
